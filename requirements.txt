python -m venv .venv && source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -r requirements.txt
python laser_health_mockup.py   # or: uvicorn laser_health_mockup:app --reload --port 8000
